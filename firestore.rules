rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // All application access goes through the authenticated server.
    // Google Cloud server credentials bypass client security rules.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}

